---
name: dedup-items
description: items（配列）の各項目が existing（配列またはフリーテキスト）のいずれかと重複しているかを similarityLevel（1〜3、既定2）の基準で判定し、重複していない項目だけを返す。
argument-hint: "items: <配列>, existing: <配列 or フリーテキスト>, similarityLevel: <1/2/3 or null>"
user-invocable: true
model: haiku
---

`skill.ts` を読み、その内容に従って実行する。
