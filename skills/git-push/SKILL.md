---
name: git-push
description: 指定したディレクトリで指定ブランチを origin に push する（upstream 未追跡でも -u で安全に push する）。
argument-hint: "workingDir: <ディレクトリ>, branch: <push するブランチ名>"
user-invocable: true
model: haiku
---

`skill.ts` を読み、その内容に従って実行する。
