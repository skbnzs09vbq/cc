---
name: git-pr-merge
description: 指定したディレクトリで指定 PR を squash マージする。失敗しても例外にせず、実行結果（成功/失敗・エラーメッセージ）をそのまま返す。
argument-hint: "workingDir: <ディレクトリ>, prNumber: <PR番号>"
user-invocable: true
model: haiku
---

`skill.ts` を読み、その内容に従って実行する。
