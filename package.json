{
  "name": "claude-skills",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "biome lint . && tsc --noEmit",
    "format": "biome format .",
    "format:write": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.2",
    "typescript": "^5.7.0"
  }
}
